const chartConfig = {
  chartOptions: {
    scaleShowVerticalLines: false,
    responsive: true,
    pan: {
      enabled: true,
      mode: 'x',
    },
    zoom: {
      enabled: true,
      mode: 'x',
    },
  },
  chartLabels: [],
  chartType: '',
  chartLegend: true,
  chartData: [],
  chartColors: []
};

export default chartConfig;
